# משימת המשך: זמן הודעה ב-Firestore

## רקע

סיימתם את אפליקציית הצ'אט הכיתתית. כל ההודעות נשמרות באוסף המשותף `board` ב-Cloud Firestore. כעת נוסיף לכל הודעה חדשה זמן שליחה.

זהו תרגיל על גמישות הסכמה (schema) ב-NoSQL: באותו אוסף כבר קיימים מסמכים ישנים ללא שדה זמן, ולאחר השינוי יישמרו מסמכים חדשים עם שדה זמן. אין צורך לשנות או למחוק את המסמכים הישנים.

> **הלוח משותף לכל הכיתה.** הודעות שתשלחו נראות לכל תלמיד ומכשיר שמחוברים לאותו פרויקט Firebase. השתמשו בתוכן מתאים לשיעור.

## מטרות

1. להוסיף מאפיין זמן מסוג `Date` למודל `Message`.
2. להשתמש ב-`@ServerTimestamp` כדי ש-Firestore יקבע את זמן השרת בעת שמירת הודעה.
3. להציג הודעות ישנות ללא זמן והודעות חדשות עם זמן, בלי לגרום לקריסה.
4. להבין שמסמכים באותו אוסף NoSQL אינם חייבים להכיל בדיוק אותם שדות.

## מה עליכם לבצע

בפרויקט שלכם, הרחיבו את `Message.java`:

1. הוסיפו את ה-imports הדרושים:
   - `com.google.firebase.firestore.ServerTimestamp`
   - `java.util.Date`
   - imports של עיצוב תאריך שתבחרו למימוש שלכם, כולל `SimpleDateFormat` ו-`Locale`.
2. הוסיפו למחלקה את השדה הבא, בדיוק עם ההערה:

   ```java
   @ServerTimestamp
   private Date timestamp;
   ```

3. הוסיפו getter ו-setter לשדה. אלו הקטעים שסופקו על ידי המורה:

   ```java
   public Date getTimestamp() {
       return timestamp;
   }

   public void setTimestamp(Date timestamp) {
       this.timestamp = timestamp;
   }
   ```

4. עדכנו בעצמכם את `toString()` כך שיעמוד בדרישות ההתנהגות הבאות:
   - אם `timestamp` הוא `null`, החזירו טקסט בפורמט `sender: content`.
   - אם `timestamp` אינו `null`, עצבו אותו באמצעות `SimpleDateFormat` בתבנית `HH:mm` ועם `Locale.getDefault()`, והחזירו טקסט בפורמט `HH:mm | sender: content`.
  
```java
   -   // עיצוב התאריך לפורמט של שעות ודקות בלבד (למשל 14:30)
            SimpleDateFormat sdf = new SimpleDateFormat("HH:mm", Locale.getDefault());
            String timeString = sdf.format(timestamp);
```
אין לשנות את האוסף: גם הודעות ישנות וגם הודעות חדשות נשארות ב-`board`.

## כיצד `@ServerTimestamp` פועל

כאשר קוראים ל-`add()` על הודעה חדשה, `@ServerTimestamp` אומר ל-Firestore לכתוב את זמן השרת בשדה `timestamp`. מיד לאחר יצירת ההודעה במכשיר, ייתכן שהערך המקומי עדיין יהיה `null`; לאחר שהעדכון מהשרת חוזר, ההודעה תיטען שוב עם זמן. לכן הטיפול ב-`null` ב-`toString()` נדרש גם להודעות ישנות וגם למצב זמני זה.

## הרצה ובדיקה

1. **לפני כל שינוי ידני ב-Firestore**, פתחו את האפליקציה וצפו בהודעות הקיימות. בדקו שהודעות ישנות מוצגות ללא זמן ואינן גורמות לקריסה.
2. התקינו את הגרסה המעודכנת על שני מכשירים או על שני אמולטורים המחוברים לאינטרנט.
3. שלחו הודעה חדשה מהמכשיר הראשון. ייתכן שתראו לרגע אותה ללא זמן; לאחר סנכרון עם השרת היא אמורה להופיע עם זמן.
4. בדקו במכשיר השני שההודעה החדשה מופיעה עם אותו זמן בפורמט `HH:mm | sender: content`.
5. שלחו הודעה חדשה גם מהמכשיר השני ובדקו עדכון בזמן אמת במכשיר הראשון.
6. פתחו את Firebase Console, עברו אל **Firestore Database > Data > board**, ובדקו:
   - מסמך ישן: מכיל `sender` ו-`content`, וללא `timestamp`.
   - מסמך חדש: מכיל גם שדה `timestamp` מסוג זמן (timestamp).
7. השוו שוב בין הודעה ישנה להודעה חדשה באפליקציה: הישנה ללא זמן, והחדשה עם זמן.

## שאלות קצרות

1. מדוע אפליקציה שקוראת מאוסף NoSQL צריכה להתמודד עם שדה שאינו קיים בכל מסמך?
2. מה פירוש "schema-on-read" בהקשר של מסמכי `board` הישנים והחדשים?
3. מה היתרון בשימוש בזמן שרת ולא בזמן שמספק הטלפון של התלמיד?
4. באיזה מצב `timestamp` יכול להיות `null` גם בהודעה חדשה?
